Тема теста: {{ topic }}

Комментарий к курсу: {{ course_comment }}

Номер теста: {{ test_no }}

Общие требования:
- Верни только валидный JSON.
- Без markdown и без пояснений.
- Ровно {{ questions_count }} вопросов.
- Ровно {{ options_count }} вариантов ответа в каждом вопросе.
- В каждом вопросе ровно один правильный ответ.

Подтемы с количеством вопросов и сложностью:
{{ subtopics }}

Недавние вопросы, которые нельзя повторять:
{{ recent_tests_questions }}

Сгенерируй тест строго в JSON-формате:
{
  "topic": "{{ topic }}",
  "questions": [
    {
      "subtopic": "...",
      "text": "...",
      "options": ["...", "...", "..."],
      "correct_option_index": 0
    }
  ]
}
