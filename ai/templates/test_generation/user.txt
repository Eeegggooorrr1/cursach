Тема теста: {{ topic }}

Дополнительные указания к курсу: {{ course_prompt }}

Номер теста: {{ test_no }}

Общие требования:
- Верни только валидный JSON.
- Без markdown и без пояснений.
- Ровно {{ questions_count }} вопросов.
- Вопросы могут быть single_choice или multiple_choice.
- Для single_choice используй {{ single_choice_options_range }} вариантов ответа и один правильный вариант.
- Для multiple_choice используй {{ multiple_choice_options_range }} вариантов ответа и несколько правильных вариантов.

Подтемы с количеством вопросов и сложностью:
{{ subtopics }}

Недавние вопросы, которые нельзя повторять:
{{ recent_tests_questions }}

Сгенерируй тест строго в JSON-формате:
{
  "topic": "{{ topic }}",
  "questions": [
    {
      "subtopic": "...",
      "text": "...",
      "question_type": "single_choice",
      "options": ["...", "...", "..."],
      "correct_option_indexes": [0]
    }
  ]
}
