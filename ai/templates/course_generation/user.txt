Название курса: {{ title }}
{% if prompt %}
Дополнительные указания:
{{ prompt }}
{% endif %}

Темы курса:
{% for topic in topics -%}
- {{ topic }}
{% endfor %}

Требования:
- сохрани все темы;
- при генерации строго следуй дополнительным указаниям, если они присутствуют;
- для каждой темы создай ровно {{ subtopics_count }} подтем;
- подтемы должны быть разными и не должны повторять друг друга;
- тема должна содержать смысловое разложение на подтемы;
- не добавляй лишних тем;
- верни результат строго в JSON.

Формат ответа:
{
  "title": "...",
  "topics": [
    {
      "name": "...",
      "subtopics": [
        { "name": "..." }
      ]
    }
  ]
}
